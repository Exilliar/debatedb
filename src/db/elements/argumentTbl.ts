export interface AddArgumentTbl {
  title: string;
  description: string;
  generalnotes: string;
  infoid: number;
  debateid: number;
}

export default interface ArgumentTbl {
  id: number;
  title: string;
  description: string;
  generalnotes: string;
  infoid: number;
  debateid: number;
}
